services:

 
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: dinostar-frontend
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    restart: unless-stopped

  

